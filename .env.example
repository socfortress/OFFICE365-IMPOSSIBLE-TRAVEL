# Impossible Travel Detection Configuration

# Time window for impossible travel detection (in minutes)
# If a user logs in from different locations within this time window, it's flagged as impossible travel
# Example: If set to 5, and a user logs in from USA then Canada 3 minutes later, it's impossible travel
IMPOSSIBLE_TRAVEL_TIME_WINDOW=5

# Maximum number of login records to keep per user
MAX_RECORDS_PER_USER=10

# Minimum distance in km to consider as a different location (when within same country)
# If users are in different countries, any distance triggers detection
# If same country, must be at least this distance apart
MIN_DISTANCE_KM=100

# Database path
DATABASE_PATH=./data/impossible_travel.db

# API Configuration
API_HOST=0.0.0.0
API_PORT=80

# Log Level
LOG_LEVEL=INFO
